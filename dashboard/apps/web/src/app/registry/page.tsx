import { PlaceholderPage } from '@/components/placeholder-page';

export default function RegistryPage() {
  return (
    <PlaceholderPage
      title="Registry"
      description="Container image registry management, scanning, and lifecycle"
      module="registry"
    />
  );
}
