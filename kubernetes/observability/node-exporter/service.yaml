apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  labels:
    app.kubernetes.io/name: node-exporter
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 9100
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: node-exporter
