apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: aegisai-base

commonLabels:
  aegisai.io/managed-by: terraform
  aegisai.io/platform: aegisai

commonAnnotations:
  aegisai.io/component: base

resources:
  - common-labels.yaml
  - common-annotations.yaml