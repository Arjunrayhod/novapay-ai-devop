apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  annotations:
    aegisai.io/component: argo-rollouts
  name: argo-rollouts-install

namespace: argo-rollouts

resources:
  - namespace.yaml
  - manifests/install.yaml
