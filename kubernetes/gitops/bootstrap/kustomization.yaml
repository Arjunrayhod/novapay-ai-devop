apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  annotations:
    aegisai.io/component: argocd-bootstrap
  name: argocd-bootstrap

resources:
- root.yaml

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/part-of: argocd
