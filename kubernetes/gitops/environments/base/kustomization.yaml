apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  annotations:
    aegisai.io/component: environment-base
    aegisai.io/environment: base
  name: environment-base

resources:
- project.yaml

labels:
- includeSelectors: true
  pairs:
    aegisai.io/environment: base
