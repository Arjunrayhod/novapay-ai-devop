apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  annotations:
    aegisai.io/component: environment-dev
    aegisai.io/environment: dev
  name: environment-dev

resources:
- project.yaml

labels:
- includeSelectors: true
  pairs:
    aegisai.io/environment: dev
