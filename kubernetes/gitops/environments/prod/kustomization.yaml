apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  annotations:
    aegisai.io/component: environment-prod
    aegisai.io/environment: prod
  name: environment-prod

resources:
- project.yaml

labels:
- includeSelectors: true
  pairs:
    aegisai.io/environment: prod
