apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: environment-staging
  namespace: argocd
  labels:
    app.kubernetes.io/name: environment-staging
    app.kubernetes.io/part-of: platform
  annotations:
    argocd.argoproj.io/sync-wave: '5'
spec:
  project: environment-staging
  source:
    repoURL: https://github.com/novapay/novapay-ai-devops
    targetRevision: HEAD
    path: kubernetes/envs/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
