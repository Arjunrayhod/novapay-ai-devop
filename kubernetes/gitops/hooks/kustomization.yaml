apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: argocd-sync-hooks
  annotations:
    aegisai.io/component: argocd-hooks

resources:
  - presync
  - sync
  - postsync
