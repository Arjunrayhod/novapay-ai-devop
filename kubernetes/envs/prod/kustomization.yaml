apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: aegisai-prod
  annotations:
    aegisai.io/environment: prod

commonLabels:
  aegisai.io/environment: prod

commonAnnotations:
  aegisai.io/environment-tier: production

resources:
  - ../../base
  - ../../namespaces
  - ../../security
  - ../../policies
  - ../../storage
  - ../../networking
  - ../../observability
  # managed by individual ArgoCD Applications in kubernetes/gitops/applications/

# patches moved to individual ArgoCD Application specs
