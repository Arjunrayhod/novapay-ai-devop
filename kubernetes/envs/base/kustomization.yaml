# Common base patches applied to all environment overlays.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: aegisai-env-base

commonLabels:
  aegisai.io/managed-by: terraform
  aegisai.io/platform: aegisai

commonAnnotations:
  aegisai.io/component: platform
